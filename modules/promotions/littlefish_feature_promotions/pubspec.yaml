name: littlefish_feature_promotions
description: Promotions feature package for littlefish merchant app. Contains promotions, discounts, and marketing-related business logic.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.8.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  # Redux state management
  redux: 5.0.0
  redux_thunk: 0.4.0
  flutter_redux: 0.10.0

  # Littlefish core dependencies
  littlefish_core: ^4.2.0
  littlefish_core_utils: ^4.2.3
  littlefish_feature_models:
    path: ../../shared/littlefish_feature_models

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
